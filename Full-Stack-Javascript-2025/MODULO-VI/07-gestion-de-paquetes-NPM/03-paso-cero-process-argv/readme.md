# Paso 0 - ENTENDER `process.argv`
## Objetivo
Ver exactamente `Qué recibe Node` cuando ejecutamos un archivo por consola

## Ejecutar 
```bash
node paso0.js
node paso0.js hola mundo
node paso0.js sumar 10 25 --verbose
```