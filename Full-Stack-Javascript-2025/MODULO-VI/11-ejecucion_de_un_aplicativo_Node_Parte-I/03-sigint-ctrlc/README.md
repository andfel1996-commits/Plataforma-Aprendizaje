# 03 - Ctrl + C (SIGINT) y cierre limpio

## Objetivo
- Mantener un proceso corriendo (con setInterval).
- Capturar Ctrl + C (SIGINT) para cerrar de forma ordenada.

## Ejecutar
```bash
npm install
npm start
```

Luego presiona **Ctrl + C**.
